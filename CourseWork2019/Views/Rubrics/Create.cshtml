@model CourseWork2019.Models.Rubric

@{
    ViewData["Title"] = "Создать рубрику";
}

<h1>Создать рубрику</h1>

<h4>Рубрика:</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RubricName" class="control-label"></label>
                <input asp-for="RubricName" class="form-control" />
                <span asp-validation-for="RubricName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Вернуться к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
